.codex/
.forge/
.forge-*-state.json
.evaluate-state.json
**/.evaluate-state.json
.pytest_cache/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.DS_Store
graphify-out/

# Local PyPI extract trees (not the live forge_next/ package)
forge_next-0.14.*/

# Local Cursor hooks (forge cursor-subagent-hooks writes; not repo config)
.cursor/hooks.json

# Skylos CLI local cache / scan output
.skylos/

# PyPI upload secrets (never commit)
.pypirc
pypi-token.env
