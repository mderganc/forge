---
name: forge:design
description: Investigate, brainstorm solutions, and write a named design spec at docs/forge/specs/ (medium/large).
---

Do not modify tracked files without user permission. Spec gate for medium/large scope.

When dispatching sub-agents, use only **`templates/forge-agent-roster.md`** roles (e.g. **Architect**, **Investigator**) — never invent `backend-architect` or layer-prefixed composites.

When `graphify-out/` exists, read `graphify-out/GRAPH_REPORT.md` before search; refresh at ship (`forge ship --step 1`).

<invoke cmd="forge design" />
