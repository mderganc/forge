---
name: forge:sketch
description: Organize intent and open decisions before design (optional CONTEXT.md/ADRs).
---

Do **not** write `docs/forge/specs/*-design.md` in sketch.

**No agent team:** 1:1 dialogue only — do not spawn Forge roster agents or Task sub-agents (`templates/forge-agent-roster.md`).

**Iterative dialogue:** re-run step 2 to continue; step 3 only after user confirms.

When `graphify-out/` exists, read `graphify-out/GRAPH_REPORT.md` before search; refresh at ship (`forge ship --step 1`).

<invoke cmd="forge sketch" />
