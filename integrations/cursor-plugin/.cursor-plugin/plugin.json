{
  "name": "forge",
  "version": "0.9.9",
  "description": "Forge workflow commands for Cursor. Requires the `forge` CLI on PATH (pipx install forge-next).",
  "components": {
    "commands": {
      "include": [
        "commands/**/*.md"
      ]
    }
  }
}

